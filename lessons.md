# lessons.md — 战术踩坑

> 记录实操层 bug、漏字段、命名混淆、工具坑。< 200 行，超则季度归档至 `archive/`。
> 格式：时间倒序追加（最新在上）。战略级失败（方向选错）不在此文，归 `failures.md`。

## 2026-08-23 Swift 分组引擎：两处"边界值桥接"陷阱

### ① 单元素 hash bucket 被 guard count > 1 丢弃 → 独立照片全丢

**现象**：`GroupingEngine.group` 对每个 hash bucket 处理时，先写了 `guard indices.count > 1`——单元素桶直接 `continue` 跳过。结果所有"没有相似者"的孤立照片全部从输出消失，UI 层拿不到完整清单。

**根因**：把"单元素桶 = 没重复 = 不用处理"当默认，忘了分组引擎的契约是**输出全部照片**（含单张组），是否展示由上层决定。分桶算法的桶大小=1 是合法输出，不是异常分支。

**修复**：单元素桶不再跳过，直接 `groups.append(makeGroup(indices, from: bucket))` 成单张组（`Grouping.swift:98-102`）。

**触发规则**：写"按某种 key 分桶后处理"的逻辑时，桶大小为 1 必须和正常桶一样走输出路径，除非明确声明"只输出 ≥2 的组"。加 `guard count > 1` 前先问一句：桶里只有 1 个元素时它该去哪？

### ② 测试数据 perturb 桥接：popcount 恰好落在 maxHamming 内

**现象**：同桶双子簇测试（`clusterA = 0x0000_0000_0000_0000`，本想用 `clusterB = 0x3FFF`）断言分 2 组，结果被并成 1 组。`perturb(0x3FFF, 2) = 0x3FFF ^ 0b11 = 0x3FFC`，而 `popcount(0x3FFC) = 12`——恰好 ≤ maxHamming=12，于是 `hamming(0, 0x3FFC) = 12` 判定"相似"，本应独立的两个集群被桥接合并。

**根因**：用"低 N 位全 1"构造差异簇时，`perturb`（翻转低 n 位）会把全 1 的尾部翻成 0，得到的值 popcount = N−1，恰好掉进阈值内。测试者凭直觉以为"差异很大"，实际被边界值击穿——汉明距离只数位差，不关心差的哪几位。

**修复**：clusterB 改为 `0x07FF_FFFF`（低 23 位全 1），其扰动值 popcount = 21 > 12，簇间最小汉明 21，留出安全余量（`GroupingTests.swift:114`、`main.swift:152`）。

**触发规则**：构造汉明距离测试数据时，别凭"看着差得多"下结论——**实际算一遍扰动后的 popcount 再断言**，并留安全余量（不要恰好在阈值边界构造正例/反例）。边界值恰好等于阈值的情形最隐蔽，跑通了反而埋雷。
