# 环境变量模板 —— 复制为 .env 后填入真实值。
# 本文件只声明变量名，不含任何真实密钥；真实 .env 已被 .gitignore 忽略，永不提交。

# SnapTidy 当前为纯本地工具（Python 去重 + iOS 分组引擎），无外部服务密钥依赖。
# 未来若引入需要密钥的服务，把变量名加在下方（本地 .env 中填真实值），勿在此填。

# 示例（按需重命名为真实变量名后取消注释）：
# MY_API_KEY=
# MY_SERVICE_SECRET=
